package com.tainin.composablepegboard.pegboard

import androidx.compose.ui.graphics.Color

private const val HIGHLIGHT_ALPHA = 0.3f

val whiteHighlight = Color.White.copy(alpha = HIGHLIGHT_ALPHA)
val redHighlight = Color.Red.copy(alpha = HIGHLIGHT_ALPHA)
val greenHighlight = Color.Green.copy(alpha = HIGHLIGHT_ALPHA)
val blueHighlight = Color.Blue.copy(alpha = HIGHLIGHT_ALPHA)